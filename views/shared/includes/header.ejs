<header>
    <nav class="container relative mx-auto py-3 text-black">
        <div class="flex items-center justify-between">
          <!-- Logo  -->
          <div class="z-30 w-48"><img src="img/logo-light.svg" /></div>
          <!-- Mid menu  -->
          <div class="flex justify-around space-x-10 text-xl">
            <a
              href="#"
              class="px-4 py-2 rounded-3xl hover:bg-slate-300 hover:bg-opacity-50"
              >Xe số</a
            >
            <a
              href="#"
              class="px-4 py-2 rounded-3xl hover:bg-slate-300 hover:bg-opacity-50"
              >Xe tay ga</a
            >
            <a
              href="#"
              class="px-4 py-2 rounded-3xl hover:bg-slate-300 hover:bg-opacity-50"
              >Xe tay côn</a
            >
            <a
              href="#"
              class="px-4 py-2 rounded-3xl hover:bg-slate-300 hover:bg-opacity-50"
              >Phân khối lớn</a
            >
          </div>
          <!-- Right menu  -->
          <div class="flex justify-between space-x-2">
            <a
              href="#"
              class="w-14 rounded-full hover:bg-slate-300 hover:bg-opacity-50"
              ><img src="img/magnifier.svg"
            /></a>
            <a
              href="/cart"
              class="w-14 rounded-full hover:bg-slate-300 hover:bg-opacity-50"
              ><img src="img/cart.svg"
            /></a>
            <% if (locals.isAuth) { %>
            <form action="/logout" method="POST"
              class="w-14 rounded-full hover:bg-slate-300 hover:bg-opacity-50"
              ><img src="img/logout.svg"
            /></form>
            <% } else { %>
              <a
                href="/signup"
                class="w-14 rounded-full hover:bg-slate-300 hover:bg-opacity-50"
                ><img src="img/user.svg"
              /></a>
            <% } %>
          </div>
        </div>
      </nav>
</header>